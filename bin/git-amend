#!/bin/bash
# h/t Mark Sandstrom for the idea
commit="$(git super-rev-parse "$1")"
if [ $? -gt 0 ]; then
    echo "usage: git amend <commit>"
    exit $?
fi

EDITOR="sed -i '/^pick $commit/a x git gsu\nx arc amend'" git rebase -i "$commit^"
